{
  "section_exercises": [
    {
      "section_title": "Defining a Custom Function",
      "text": "Create a function called `greet` that takes a name as an argument and prints a personalized greeting. For example, if the name is 'Alice', the function should print 'Hello, Alice!'.",
      "solution": "greet <- function(name) {\n    print(paste('Hello,', name, '!'))\n}\n\n# Test the function\ngreet('Alice')",
      "output": "[1] \"Hello, Alice!\"",
      "hints": [],
      "explanation": "The function `greet` takes a parameter `name` and uses the `paste` function to concatenate the greeting message. The `print` function is used to display the message."
    },
    {
      "section_title": "Function Arguments",
      "text": "Create a function called `get_average` that takes a vector of numbers as an argument and returns the average of the numbers. For example, if the vector is `c(1, 2, 3, 4, 5)`, the function should return `3`.",
      "solution": "get_average <- function(numbers) {\n    return(mean(numbers))\n}\n\n# Test the function\nget_average(c(1, 2, 3, 4, 5))",
      "output": "[1] 3",
      "hints": [],
      "explanation": "The function `get_average` takes a parameter `numbers` and uses the `mean` function to calculate the average of the numbers. The `return` statement is used to return the result."
    },
    {
      "section_title": "If/Else Statements",
      "text": "Write an if/else statement that checks if a number is positive, negative, or zero. If the number is positive, print 'Number is positive'. If the number is negative, print 'Number is negative'. If the number is zero, print 'Number is zero'. Test your if/else statement with different values of `num`.",
      "solution": "num <- -2\nif (num > 0) {\n   print('Number is positive')\n} else if (num < 0) {\n   print('Number is negative')\n} else {\n   print('Number is zero')\n}",
      "output": "[1] \"Number is negative\"",
      "hints": [],
      "explanation": "The if/else statement checks the value of `num` and prints the appropriate message based on whether it is positive, negative, or zero."
    },
    {
      "section_title": "For Loops",
      "text": "Use a for loop to print the numbers from 1 to 5.",
      "solution": "for (i in 1:5) {\n   print(i)\n}",
      "output": "[1] 1\n[1] 2\n[1] 3\n[1] 4\n[1] 5",
      "hints": [],
      "explanation": "The for loop iterates over the range 1 to 5 and prints each value."
    },
    {
      "section_title": "While Loops",
      "text": "Use a while loop to print the numbers from 0 to 4.",
      "solution": "num <- 0\nwhile (num < 5) {\n    print(num)\n    num <- num + 1\n}",
      "output": "[1] 0\n[1] 1\n[1] 2\n[1] 3\n[1] 4",
      "hints": [],
      "explanation": "The while loop continues to execute as long as `num` is less than 5. The value of `num` is printed and then incremented by 1 in each iteration."
    },
    {
      "section_title": "Putting It All Together",
      "text": "In the placeholder code, write a function called `calculate_mean` that takes a dataset as an argument and calculates the mean of a specified column. The function should have two parameters: `data` and `column`. Use a for loop and if/else statements to iterate over the rows of the dataset and calculate the mean of the specified column. Return the mean.",
      "solution": "calculate_mean <- function(data, column) {\n    total <- 0\n    count <- 0\n    for (i in 1:nrow(data)) {\n        if (!is.na(data[i, column])) {\n            total <- total + data[i, column]\n            count <- count + 1\n        }\n    }\n    if (count > 0) {\n        mean_value <- total / count\n        return(mean_value)\n    } else {\n        return(NA)\n    }\n}",
      "output": null,
      "hints": [],
      "explanation": "The function `calculate_mean` takes a dataset `data` and a column name `column` as parameters. It initializes variables `total` and `count` to keep track of the sum and count of non-missing values. The for loop iterates over the rows of the dataset. If the value in the specified column is not missing, it adds the value to the total and increments the count. After the loop, it calculates the mean if there are non-missing values, otherwise it returns `NA`."
    }
  ],
  "lesson_exercises": []
}