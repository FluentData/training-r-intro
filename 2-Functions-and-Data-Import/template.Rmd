{
  "section_exercises": [
    {
      "section_title": "R's Built-in Functions",
      "text": "Create a vector `numbers` with the numbers 2, 4, 6, and 8. Use the `mean()` function to calculate the average of the `numbers` vector.",
      "solution": "numbers <- c(2, 4, 6, 8)\nmean(numbers)",
      "output": "5",
      "hints": [
        "Remember to assign the vector to a variable using the `<-` operator.",
        "Check the documentation for the `mean()` function if you're not sure how to use it."
      ],
      "explanation": "The `mean()` function calculates the average of a vector. In this case, it calculates the average of the `numbers` vector, which is 5."
    },
    {
      "section_title": "Nesting Functions",
      "text": "Use the `subset()` and `mean()` functions to calculate the average temperature for the month of July in the `chicago_air` dataset.",
      "solution": "mean(subset(chicago_air, month == 7)$temp)",
      "output": "73.2",
      "hints": [
        "Make sure to load the `chicago_air` dataset before running the code.",
        "Use the `subset()` function to extract the temperature data for the month of July.",
        "Then use the `mean()` function to calculate the average temperature."
      ],
      "explanation": "The `subset()` function filters the `chicago_air` dataset to only include observations where the `month` variable is equal to 7 (July). The `$` operator is used to access the `temp` variable from the subsetted dataset. Finally, the `mean()` function calculates the average temperature for the subsetted dataset, which is 73.2."
    },
    {
      "section_title": "CSV Import",
      "text": "Use the `read.csv()` function to import the `chicago_air.csv` file into R. Assign the resulting dataframe to the variable `chicago_air_csv`.",
      "solution": "chicago_air_csv <- read.csv('../data/chicago_air.csv')",
      "output": "",
      "hints": [
        "Make sure to provide the correct file path to the `read.csv()` function.",
        "Use the assignment operator `<-` to assign the imported dataframe to the variable `chicago_air_csv`."
      ],
      "explanation": "The `read.csv()` function is used to import the contents of the `chicago_air.csv` file into R. The resulting dataframe is assigned to the variable `chicago_air_csv`."
    },
    {
      "section_title": "Excel Import",
      "text": "Uncomment the lines of code to install and load the `readxl` library. Then, use the `read_excel()` function to import the `chicago_air.xlsx` file into R. Assign the resulting dataframe to the variable `chicago_air_excel`.",
      "solution": "# install.packages('readxl')\n# library(readxl)\n\n# chicago_air_excel <- read_excel('../data/chicago_air.xlsx')",
      "output": "",
      "hints": [
        "Remove the `#` character to uncomment the lines of code.",
        "Make sure to provide the correct file path to the `read_excel()` function.",
        "Use the assignment operator `<-` to assign the imported dataframe to the variable `chicago_air_excel`."
      ],
      "explanation": "The `read_excel()` function from the `readxl` library is used to import the contents of the `chicago_air.xlsx` file into R. The resulting dataframe is assigned to the variable `chicago_air_excel`."
    }
  ]
}